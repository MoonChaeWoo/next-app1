version: '3.9'

services:
  postgres:
    image: postgres:16.1
    restart: always
    volumes:
      - ./16_1:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    container_name: Postgres16